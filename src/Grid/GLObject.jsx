export class GLObject {

    static dataGetter;

    static gridCreator;

    static menuId;

    static entityInfo = {};

    static gridSettings = {};

    static appSettings = {};

    // 0 - PostgreSQL, 1 - MSSQL, 2 - ORACLE
    static serverType = 0;
}